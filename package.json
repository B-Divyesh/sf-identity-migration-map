{
  "name": "identity-migration-map-site",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "scripts": {
    "dev": "vite --config site/vite.config.ts",
    "test": "cargo test && cargo build --release && npm run typecheck && npm run build:site && node --test site/tests/*.test.mjs && playwright test",
    "test:claims": "playwright test",
    "typecheck": "tsc --noEmit --target es2022 --module esnext --moduleResolution bundler --lib ES2022,DOM --strict site/main.ts",
    "build:cli": "cargo build --release",
    "build:site": "vite build --config site/vite.config.ts && node site/build-service-worker.mjs",
    "build": "npm run build:cli && npm run build:site",
    "preview:site": "vite preview --config site/vite.config.ts"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.13.0",
    "@playwright/test": "1.58.2",
    "playwright": "1.58.2",
    "typescript": "^5.9.2",
    "vite": "^7.1.3"
  }
}
